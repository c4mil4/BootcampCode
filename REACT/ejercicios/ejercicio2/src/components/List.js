export default function List(props){
    return(
        <div>
            <h1>{props.category}</h1>
            <ul>
            <li>{props.id, props.product}</li>

            </ul>
        </div>
    )
}